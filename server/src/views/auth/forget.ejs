<%- include('../layout/auth/header.ejs') %>
  <div class="sign section--bg" data-bg="img/section/section.jpg">
    <div class="container">
      <div class="row">
        <div class="col-12">
          <div class="sign__content">
            <!-- authorization form -->
            <form method="POST" class="sign__form">
              <a href="/" class="sign__logo">
                <img src="img/logo.svg" alt="" />
              </a>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <div class="sign__group">
                <input type="text" class="sign__input" placeholder="Email" required name="email" />
              </div>

              <button class="sign__btn" type="submit">send reset link</button>

              <span class="sign__text">We will send a reset link to your Email</span>
            </form>
            <!-- end authorization form -->
          </div>
        </div>
      </div>
    </div>
  </div>

  <%- include('../layout/auth/footer.ejs') %>